<!DOCTYPE html>
<html>
<head>
	<title><%= title; %> | <%= name; %></title>
	<meta name="description" content="<%= description; %>">
	<% if ( environment !== 'production' ) { %>
		<% public.static.css._contents.forEach(( asset ) => { %>
			<link rel="stylesheet" type="text/css" href="<%=uri;%>/static/css/<%=asset;%>">
		<% }); %>
	<% } else { %>
		<link rel="stylesheet" type="text/css" href="<%=uri;%>/static/css/main.css">
	<% } %>
</head>
<body>
	<%- partial("../_partials/_header"); %>
	<div class="main-wrapper">
		<div class="menu-category-wrapper">
			<%- partial("/_partials/header_image", {
				image_src : public['menu-categories']._data[current.source]['category-header-image'],
				menu_category_text : current.source
			});
			%>
			<div class="menu-category-content-wrapper">
				<%- yield; %>
			</div>
		</div>
		<div class="main-wrapper-background">
			<%- partial("../_partials/_backgroundWrapper"); %>
		</div>
		<div id="loading-dock" class="loading"><div class="loading-dock-content"><img src="<%=uri;%><%=logoImage;%>"></div></div>
	</div>
	<%- partial("../_partials/_footer"); %>
	<% public.static.js._contents.forEach(( asset ) => { %>
		<script type="text/javascript" src="<%=uri;%>/static/js/<%=asset;%>"></script>
	<% }); %>
</body>
</html>
